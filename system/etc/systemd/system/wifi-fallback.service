[Unit]
Description=Check known wifi networks and fall back to hotspot if none in range
Wants=network.target
After=network.target NetworkManager.service

[Service]
Type=oneshot
ExecStart=/usr/local/bin/wifi-fallback.sh
